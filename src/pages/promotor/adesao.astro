---
export const prerender = false;

const search = Astro.url.search;
const params = new URLSearchParams(search);
params.set("role", "promotor");

return Astro.redirect(`/autenticacao/login?${params.toString()}`, 307);
---
